# =============================================================================
# Cloud File Converter — Render.com Deployment Blueprint
# =============================================================================
# Deploy this project on Render with one click using this file.
# Docs: https://render.com/docs/blueprint-spec

services:
  # ── Django API ─────────────────────────────────────────────────────────────
  - type: web
    name: cloudfileconverter-api
    env: python
    region: oregon
    plan: starter
    buildCommand: |
      pip install -r requirements/production.txt &&
      python manage.py collectstatic --noinput &&
      python manage.py migrate --noinput
    startCommand: gunicorn config.asgi:application -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    rootDir: backend
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        value: cloudfileconverter-api.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: cloudfileconverter-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_STORAGE_BUCKET_NAME
        value: cloud-file-converter-prod
      - key: AWS_S3_REGION_NAME
        value: us-east-1
      - key: GEMINI_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        value: https://cloudfileconverter.vercel.app
      - key: EMAIL_HOST
        value: smtp.sendgrid.net
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_HOST_USER
        value: apikey
      - key: EMAIL_HOST_PASSWORD
        sync: false

    healthCheckPath: /api/v1/health/

  # ── Celery Worker ──────────────────────────────────────────────────────────
  - type: worker
    name: cloudfileconverter-celery-worker
    env: python
    region: oregon
    plan: starter
    buildCommand: pip install -r requirements/production.txt
    startCommand: celery -A config.celery worker --loglevel=info --concurrency=2
    rootDir: backend
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: DATABASE_URL
        fromDatabase:
          name: cloudfileconverter-db
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_STORAGE_BUCKET_NAME
        value: cloud-file-converter-prod
      - key: GEMINI_API_KEY
        sync: false

  # ── Celery Beat (Scheduler) ────────────────────────────────────────────────
  - type: worker
    name: cloudfileconverter-celery-beat
    env: python
    region: oregon
    plan: starter
    buildCommand: pip install -r requirements/production.txt
    startCommand: celery -A config.celery beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    rootDir: backend
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: DATABASE_URL
        fromDatabase:
          name: cloudfileconverter-db
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: cloudfileconverter-redis
          property: connectionString

# ── Managed Databases ─────────────────────────────────────────────────────────
databases:
  - name: cloudfileconverter-db
    databaseName: cloudconv_prod
    user: cloudconv_prod
    plan: starter

  - name: cloudfileconverter-redis
    plan: starter
